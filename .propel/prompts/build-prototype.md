# build-prototype

## Overview
As a Solution Architect, build a functional prototype that validates a given business hypothesis using priority-based scoping. Critical and high-priority features are implemented as working code, while lower-priority features are delivered as design blueprints.

## Execution
Call MCP tool:
    - ReadPrompt(name="build-prototype", version="latest")

- Update ToDo list derived from the returned prompt instructions by readjusting the items.
