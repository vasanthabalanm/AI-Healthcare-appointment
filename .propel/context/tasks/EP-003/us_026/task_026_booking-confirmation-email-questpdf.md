# Task - TASK_026

## Requirement Reference

- **User Story**: US_026 — Booking confirmation email + QuestPDF
- **Story Location**: `.propel/context/tasks/EP-003/us_026/us_026.md`
- **Parent Epic**: EP-003

### Acceptance Criteria Addressed

| AC ID | Description |
|-------|-------------|
| AC-001 | Confirmation email sent via MailKit SMTPS port 465 |
| AC-002 | PDF confirmation generated by QuestPDF in < 2s, < 200KB, no temp file write |
| AC-003 | PDF attached to email; if PDF generation fails, email is still sent without attachment |
| AC-004 | Hangfire retries: 3 attempts with exponential backoff on email send failure |
| AC-005 | `SendConfirmationEmailJob` Hangfire job executes full send logic |

### Edge Cases

- QuestPDF throws → catch exception; log WARNING; send email without PDF attachment
- MailKit SMTPS fails all 3 retries → job moves to Hangfire dead-letter

---

## Design References

N/A — UI Impact: No

---

## AI References

N/A — AI Impact: No

---

## Mobile References

N/A — Mobile Impact: No

---

## Applicable Technology Stack

| Layer | Technology | Version | Justification |
|-------|-----------|---------|---------------|
| Backend | MailKit | 4.x | SMTPS email delivery per design.md |
| Backend | QuestPDF | 2024.x | PDF generation per design.md |
| Backend | Hangfire | 1.8.x | Job queue + retry per design.md |

---

## Task Overview

Implement `SendConfirmationEmailJob` as a full Hangfire background job. Generate a QuestPDF appointment confirmation (in-memory, no temp file). Attach to email via MailKit SMTPS. Fallback: send without PDF if generation fails. 3× exponential retry.

---

## Dependent Tasks

- **TASK_001 (us_004)** — Hangfire infrastructure
- **TASK_001 (us_012)** — `IEmailService` / `MailKitEmailService`
- **TASK_001 (us_019)** — `SendConfirmationEmailJob` stub to be replaced

---

## Impacted Components

- `src/ClinicalHealthcare.Infrastructure/Jobs/SendConfirmationEmailJob.cs`
- `src/ClinicalHealthcare.Infrastructure/Pdf/ConfirmationPdfGenerator.cs`

---

## Implementation Plan

1. Add `QuestPDF` NuGet to `ClinicalHealthcare.Infrastructure`.
2. Create `ConfirmationPdfGenerator`: `Generate(AppointmentDto dto) → byte[]`; build PDF in-memory using QuestPDF fluent API; no `File.WriteAllBytes`; benchmark: must complete < 2s; output < 200KB.
3. Implement `SendConfirmationEmailJob.Execute(appointmentId)`:
   - Load `Appointment` + `UserAccount` (patient) from DB.
   - Try `ConfirmationPdfGenerator.Generate(...)` → catch → log WARNING, set `pdfBytes = null`.
   - Build email with MailKit; if `pdfBytes != null`, attach `MimePart` with `application/pdf`.
   - Send via SMTPS port 465.
4. Decorate job with `[AutomaticRetry(Attempts = 3)]` (overrides global default if needed).
5. Replace stub `SendConfirmationEmailJob` from us_019.

---

## Current Project State

```
src/ClinicalHealthcare.Infrastructure/Jobs/
├── SendConfirmationEmailJob.cs  (stub)
└── SendReminderJob.cs  (stub)
```

---

## Expected Changes

| Action | File Path | Description |
|--------|-----------|-------------|
| MODIFY | `src/ClinicalHealthcare.Infrastructure/Jobs/SendConfirmationEmailJob.cs` | Replace stub with full implementation |
| CREATE | `src/ClinicalHealthcare.Infrastructure/Pdf/ConfirmationPdfGenerator.cs` | QuestPDF in-memory PDF generation |

---

## External References

- [QuestPDF Documentation](https://www.questpdf.com/documentation/)
- [MailKit SMTPS](https://github.com/jstedfast/MailKit/blob/master/FAQ.md)
- [Hangfire AutomaticRetry](https://docs.hangfire.io/en/latest/background-jobs/retrying.html)

---

## Build Commands

```bash
dotnet add src/ClinicalHealthcare.Infrastructure package QuestPDF
dotnet build
```

---

## Implementation Validation Strategy

- Trigger job manually in Hangfire dashboard → email received with PDF attachment.
- Force QuestPDF to throw → email received without attachment; WARNING in logs.
- PDF size < 200KB; generation time < 2s.
- No files written to disk during PDF generation.
- 3 SMTP failures → job in dead-letter queue.

---

## Implementation Checklist

- [x] **[AC-001]** Email sent via MailKit SMTPS port 465
- [x] **[AC-002]** QuestPDF generates PDF in-memory (<2s, <200KB, no temp file)
- [x] **[AC-003]** PDF generation failure → email sent without attachment (fallback)
- [x] **[AC-004]** `[AutomaticRetry(Attempts=3)]` on job
- [x] **[AC-005]** `SendConfirmationEmailJob` is a proper Hangfire background job
- [x] `ConfirmationPdfGenerator` uses QuestPDF fluent API
- [x] SMTP credentials from env vars only
- [x] `dotnet build` passes with 0 errors
